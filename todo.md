# SiftPipe — Master TODO

Consolidated from [readme.md](readme.md) §7 and [AWS_HOSTING_TODO.md](AWS_HOSTING_TODO.md), plus a few compliance action items that only existed in chat until now. Update this file as items get done — the two source docs stay the detailed reference, this one is the at-a-glance list.

## A. Code changes to build (from AWS_HOSTING_TODO.md §1)

- [x] Playwright headless via env var — [blocks/dynamic_analysis.py](blocks/dynamic_analysis.py), [blocks/dynamic_injector.py](blocks/dynamic_injector.py) (`PLAYWRIGHT_HEADLESS`, default `false`)
- [x] `networkidle` → `domcontentloaded` fix in both files' navigation calls — all 102 tests pass
- [x] Record video per finding — B7 gives each payload its own context (`storage_state` reuse for login, no re-login per payload) so each finding gets `results/videos/{pid}.webm`, matching the existing `screenshot_{pid}.png` pattern. B4 records one video for the whole discovery session (`results/videos/b4_discovery.webm`), since there's no per-finding concept there.
- [x] Static-file endpoint in `api.py` — mounted `/media` → `results/` via `StaticFiles`, smoke-tested with a real request (200 for an existing file, 404 for a missing one)
- [x] Frontend "watch it run" video player next to B8/B9 results — also fixed the older gap where B7 screenshots weren't rendering as images at all. Required a fix along the way: `video_path` wasn't being propagated B7 → B8 → B9 (only `screenshot_path` was), so that's patched in `blocks/analyze_results.py` and `blocks/correlate_results.py` too. `tsc --noEmit` and `eslint` both clean.
- [x] Run history: `blocks/run_history.py` (SQLite, kept at the project root — deliberately outside `results/` since `fresh_reset()` wipes that folder wholesale) + `GET /api/runs`/`GET /api/runs/{id}` + a "Past Runs" tab reusing `Section`/`FindingRow`/mappers with historical data. 7 new backend tests, 109/109 total passing, `tsc`/`eslint` clean.
- [x] `VITE_API_BASE` configurable in [ui/src/lib/api.ts](ui/src/lib/api.ts), with a `vite-env.d.ts` type declaration added
- [x] CORS allow-list — `FRONTEND_ORIGIN` env var in `api.py`, comma-separated, appended to the always-allowed localhost origins
- [x] Shared-secret check on `/api/environment/reset`, `/api/run`, `/api/validate`, `/api/reset` via `require_api_key` — no-op when `SIFTPIPE_API_KEY` is unset (confirmed via direct HTTP smoke test: open when unset, 401 on missing/wrong key, 200 on correct key), frontend sends it via `VITE_API_KEY`
- [ ] `playwright install --with-deps chromium` (server-install step, not code, but easy to forget)
- [ ] `git submodule update --init --depth 1` after cloning on the server

## B. Older pending items from readme.md §7 not yet folded into A

- [x] **§7.4 (partial):** login field selectors (`input_loginId`/`input_password-input`) had zero fallback in either B4 or B7, duplicated independently in both with a comment pointing at B4 as the "source of truth." Extracted into [blocks/mattermost_auth.py](blocks/mattermost_auth.py) — `LOGIN_ID_SELECTORS`/`PASSWORD_SELECTORS` candidate lists plus `find_working_selector()`, tried in order (current ids first, more semantic/stable attributes as fallback), used by both files now instead of two independent copies. 3 new tests. The rest of §7.4 (a fully generic, Mattermost-version-independent discovery engine) is genuinely bigger scope — see readme §8 roadmap point 1 — and stays out of scope on purpose.
- [x] **§7.8 / §7.11 (partial):** added a Fresh reset / Restore existing toggle to `Sidebar.tsx`. No backend change needed or made — there was never a real "mode" concept beyond "was `/api/environment/reset` called or not," so restore mode was already reachable (just click "Run analysis" without resetting first); this makes that choice explicit and visible instead of an undiscoverable side effect. Restore mode shows whether Mattermost is currently reachable and, if not, tells you to start it manually instead of silently doing nothing.
- [x] ~~§7.11 (rendering B7 screenshots as images)~~ — covered by item A's static-file endpoint, no separate work needed.

## C. AWS deployment steps (from AWS_HOSTING_TODO.md §2 — infra, not code)

- [ ] Set an AWS Budget alert ($10 / $25 / $50)
- [ ] Launch EC2 (t3.medium, Ubuntu 24.04, closed security group except 80/443) + allocate an Elastic IP
- [ ] One-time server setup: Docker, Python, nginx, certbot, clone + submodule, venv + deps, `.env` files
- [ ] TLS: pick a domain or use the free AWS hostname, run certbot, confirm nginx proxies to the API
- [ ] `systemd` unit for the API; confirm Mattermost's `unless-stopped` restart policy
- [ ] Deploy frontend to Cloudflare Pages with `VITE_API_BASE` set
- [ ] Go-live check: `/api/health`, one full B3→B9 dry run, then send the link
- [ ] Afterward: stop or terminate everything

## D. Explicitly not needed right now

Documented on purpose, so these don't get "rediscovered" mid-project and cause panic:

- **Pluggable LLM provider** (readme §8) — out of scope on purpose, don't touch.
- **Full multi-tenant multi-target product** (Approach B: user accounts, sign-up, per-user credentials, domain-verification/consent gating, a target picker anyone can use) — decided against for this deadline, see [MULTI_TARGET_PLAN.md](MULTI_TARGET_PLAN.md)'s "Non-goals" section for the full reasoning.
- **Always-on "production" hosting** — ruled out already; this project only ever needs the one-time demo deployment.

~~Multi-target support — out of scope~~ — **superseded 2026-08-08**: a scoped version (Approach A, a second real authorized target — NaVi-Q — via a per-target config profile, not the full product above) is now in progress. See [MULTI_TARGET_PLAN.md](MULTI_TARGET_PLAN.md).

## E. Multi-target support — Approach A (from MULTI_TARGET_PLAN.md, condensed)

Full task list with expected results per task lives in
[MULTI_TARGET_PLAN.md](MULTI_TARGET_PLAN.md) — update checkboxes there, this
is just the at-a-glance phase list.

- [x] Open questions confirmed: NaviQ test credentials, source access for B3 (still needs a manual `git clone` from you), payments app scope. Rules of engagement is the one item still genuinely open.
- [x] Phase 0 — local NaViQ instance set up, seeded, and scouted end-to-end (Task 0.1 superseded by the local-instance decision, Task 0.2 done 2026-08-09: authenticated area explored with Playwright, two JS-heavy forms found)
- [x] Phase 1 — target-profile abstraction done 2026-08-09 (`blocks/targets.py`, `--target` CLI flag, `SIFTPIPE_TARGET` env var). 115/115 tests green, Mattermost behavior confirmed unchanged. B7→B9 verified starting correctly against live Mattermost but not run to full completion (100-payload run stopped deliberately to save Groq quota) — see MULTI_TARGET_PLAN.md for the full regression-gate note, including a real Windows/Git-Bash `mkfifo` + `input()` bug hit and worked around along the way.
- [x] Phase 2 — B4 discovery generalized to a same-origin crawl done 2026-08-09 (`blocks/crawler.py`, generic BFS in `discover_attack_surface()`). Verified live against both targets: Mattermost beat its old-code baseline (13 forms/26 inputs/80 endpoints vs. 5/10/54), NaViQ crawled clean (30 forms/43 inputs, 0 errors). Two real bugs found and fixed via those live runs, not just unit tests — see MULTI_TARGET_PLAN.md. 136/136 tests green.
- [x] Phase 3 — B7 login + response-capture generalized done 2026-08-09 (highest-uncertainty phase, as predicted — ran long but the core generalization held: one unified `_is_submission_response` covers both targets). Verified live against NaViQ: real login via B7's own code path, and a real payload → real captured 200 response with a non-empty body on `/account/settings/`. Two real bugs found and fixed via those live runs (a `load_dotenv()` ordering gap in `blocks/targets.py`; a `target`/`target_profile` naming collision caught before it ran). No new Django error markers needed — confirmed by reading Django's actual templates. 140/140 tests green. See MULTI_TARGET_PLAN.md for full detail.
- [x] Phase 4 — B1/environment target-awareness done 2026-08-09. `naviq_fresh_reset()` built and verified live twice in a row (real idempotency, not just a unit test) — real migrations, real seed counts, real working login each time. `--mode fresh`/`--mode restore` and the API's reset endpoint both dispatch on target now. Found and fixed 2 real bugs via live testing (a "#unknown" selector bug in B7 that made it nearly non-functional against NaViQ; a hidden-field-targeting bug in B5 that let CSRF/redirect fields crowd out real ones). Decided (not implemented) that NaViQ's dev server stays a manual prerequisite — see MULTI_TARGET_PLAN.md for reasoning. 153/153 tests green.
- [x] Interlude (done 2026-08-10, ahead of Phase 5) — two follow-ups found by direct use, not from the original task list: (1) B7 couldn't get a real server response from multi-required-field forms without `novalidate` (client-side validation blocked the request before it was ever sent) — fixed with `_disable_client_validation()`, re-verified live against NaViQ's contact form (now a real `302` instead of a timeout). (2) `run_history`'s `runs` table had no `target` column at all, so Past Runs couldn't say which target a historical run used — fixed with a safe `ALTER TABLE` migration (verified against this project's own real `siftpipe_history.db`), and this project's 13 pre-existing rows corrected by hand using actual session knowledge. `results/` folder per-target separation deliberately deferred — bigger, touches most of the codebase's file I/O, no data loss today (just live-overwrite between runs). 158/158 tests green. See MULTI_TARGET_PLAN.md.
- [x] Phase 5 — done 2026-08-10. Went further than "read-only display" after weighing it with the user: `blocks/targets.py`'s `TargetProfile` gained `display_name`/`stack_label`; `api.py` exposes `GET/POST /api/target` (the latter guarded 409 while a run or env reset is in flight, same rule `POST /api/environment/reset` already enforces) and `ACTIVE_TARGET` is now a runtime-switchable module global instead of env-var-only; `/api/environment/health` generalized (`mattermost_up` → `target_up`, target-aware ping). TopBar.tsx got a real 2-way picker (closed set of the 2 known profiles, not a free-text "any site" input — that line is what would make it Approach B); Sidebar.tsx's Fresh/Restore toggle now genuinely respects `supports_fresh_reset` and shows target-specific prereq/manual-start copy (NaViQ's `manage.py runserver` vs Mattermost's `docker compose up -d`). Live-verified with Playwright against the real running frontend+backend — switching target in the browser actually flips every dependent bit of copy, not just the label. 166/166 tests green (8 new, covering the endpoint + its guards).
- [x] B7 auto-fill (done 2026-08-10, ahead of Phase 6) — the deferred "auto-fill a form's other fields with plausible placeholder values" item. `_fill_sibling_fields()` in `blocks/dynamic_injector.py`: fills every other empty field in the injected field's `<form>` with a heuristic placeholder (not an LLM call — keeps B5's Groq quota untouched) before submit, closing the gap `_disable_client_validation()`'s own docstring flagged in the Interlude above. Real bug found live, not hypothetical: NaViQ's contact form has a spam honeypot kept off-screen via CSS + `aria-hidden` rather than `type="hidden"` — confirmed live that Playwright's own `is_visible()` doesn't catch it, so a naive version would've filled it and gotten every payload against that form treated as bot traffic. Fixed by also skipping any field under an `aria-hidden="true"` ancestor. Verified live end to end against the real `/contact/send/` form: payload field held its exact value, honeypot stayed empty, email/name got real placeholders, and the submission got a genuine `302` instead of the earlier "Something went wrong" rejection. 171/171 tests green (5 new). `results/` folder per-target separation remains deferred (bigger scope, unrelated to this). See MULTI_TARGET_PLAN.md's Phase 3 "Addendum 2" for full detail.
- [x] B7 XSS_reflected false-positive fix (done 2026-08-10, ahead of Phase 6) — prompted by "is B7 actually finding interesting things?": checked real historical run data instead of just the code's intent, and found every one of the 8 completed real Mattermost runs sitting at `confirmed_findings: 0` despite 17-20 raw findings each. Root cause: `XSS_reflected` fired on any payload merely echoed back verbatim — including SQLi/command-injection test payloads, since Mattermost's chat legitimately echoes whatever you post in its own JSON API response. Fixed with two required gates in `blocks/dynamic_injector.py`: `_looks_like_xss_payload()` (payload must contain real HTML/JS syntax) and `_looks_like_html_response()` (response must actually look like HTML, not JSON — `_execute_one()` now captures `Content-Type`). Verified against real data: replayed the actual historical B7 findings from runs 6-8 through the corrected gates — all 3 real false positives now evaluate `False`, nothing else changes. 180/180 tests green (9 new). See MULTI_TARGET_PLAN.md's Phase 3 "Addendum 3".
- [x] B3 target-awareness (done 2026-08-10, ahead of Phase 6) — prompted by "why is it not running block 3 on naviq?", called correctly a major gap: B3 had never been generalized at all, and it wasn't just a hardcoded path — `blocks/static_scanner.py`'s extensions/directory filter were tuned to Mattermost's own Go/TypeScript stack (zero overlap with NaViQ's real Django/Python code), and `results/files_list.txt` was a single shared cache with no target in its name, so whichever target ran B3 first silently won the cache forever. Fixed: `TargetProfile` (`blocks/targets.py`) gained `source_dir`/`source_extensions`/`source_exclude_dirs`/`source_relevant_dirs` per target (NaViQ: `.py`, no directory allowlist, excludes its own real venv `naviq-src/naviq/.venv310` + `migrations/`); `run_static_analysis()` now takes a `target_profile`, both real call sites updated, cache file now target-scoped. Verified against the real `naviq-src/naviq/` checkout, not a fixture: 119 real `.py` files found (zero venv/`.go` contamination), 10 scanned through the real LLM, 8 genuine findings saved. 186/186 tests green (6 new). See MULTI_TARGET_PLAN.md's "B3 target-awareness" section.
- [x] B3 prompt precision — hardcoded-secret false positive + "not found" placeholder entries (done 2026-08-10, same session as B3 target-awareness) — the "say the word if you want that tightened too" follow-through. Root cause: `OWASP_SCOPE`'s A02 description didn't distinguish a literal secret value from an env-var read, so `ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY")` (the correct pattern) got flagged `confidence: high`. Fixed the prompt; verified against the exact real files that produced the false positives — both now return `[]`, while a genuine hardcoded Stripe-shaped key sanity check still gets caught correctly. That same verification pass surfaced a second real bug: the LLM returning "not found" placeholder entries (`"line": 0`) instead of an empty array despite the prompt already forbidding it — 3 of 5 were `medium` confidence and would've been saved as real findings. Fixed both in the prompt (explicit anti-example) and as a code-level backstop in `main.py` (reject any finding missing a real line number) since prompt compliance alone isn't reliable. 188/188 tests green (2 new). See MULTI_TARGET_PLAN.md's "B3 target-awareness" addendum.
- [x] NaViQ dev-server automation (done 2026-08-10, ahead of Phase 6) — prompted by "the jury has to be able to select the target and run the pipeline only from the frontend, can't have them doing command lines": reversed Phase 4 Task 4.3's deliberate "manual prerequisite" decision, which was correct for a developer with terminal access but a hard blocker for this new requirement. `ensure_naviq_server_running()` (`blocks/environment.py`) spawns `manage.py runserver` as a background subprocess when unreachable, idempotent (real HTTP check, not a flag), wired into `naviq_fresh_reset()` plus safety nets in both `api.py` and `main.py`. Three real bugs found live while building/verifying this: (1) the server's own log file lived under `results/`, colliding with every reset's own results-wipe (`WinError 32`) — moved to the already-gitignored `naviq-src/`; (2) a leaked file descriptor caught by a unit test's own cleanup failure, not inspection; (3) the most significant — the Run button could enable while the database was still mid-reset, since `targetUp` alone doesn't mean the reset finished (found via live Playwright testing, sampling the button's disabled state across the whole reset window — never caught this from reading the code). Also fixed a related staleness bug found along the way: the "{target} running" indicator could briefly show the *previous* target's cached status right after switching (Mattermost's Docker turned out to be genuinely running in the background, which is what made it visible). 195/195 tests green (7 new). Fully live-verified: real subprocess spawn, real HTTP reachability, idempotent re-run, and the full jury-facing browser flow with zero terminal commands. See MULTI_TARGET_PLAN.md's "NaViQ dev-server automation" section.
- [x] Phase 6 — done 2026-08-10. Task 6.1: real `--target naviq --mode restore` run, B3→B9, non-interactive (monkeypatched B6 same way earlier Windows/stdin issues were worked around, real auto-selection from B5's real output, not a stub). Real result (run id 14, 71s): `ensure_naviq_server_running()`'s restore-mode safety net fired for real; B3 found 0 findings (today's "not found" filter correctly caught every placeholder); B7's 3 contact-form payloads all got genuine `302`s, live proof `_fill_sibling_fields()` works in the full pipeline; 0 anomalies (Django auto-escaping); B9 correctly correlated 0. Task 6.2: full test suite 195/195. For Mattermost, checked with the user first since its Docker container held real data unrelated to this session (running since Aug 8) — confirmed destructive `--mode fresh` was fine. Real result (run id 15, ~2m16s): full teardown/rebuild/reseed worked exactly as before; B3 found a **genuine real hardcoded secret** (a live Giphy SDK key in Mattermost's own `default_config.ts`) correctly kept *while* suppressing several "not found" placeholders on the same file — real proof the A02 fix discriminates, doesn't just suppress; B7's 2 XSS payloads against `post_textbox` got **0 anomalies — first live proof against real Mattermost that today's `XSS_reflected` fix actually works**, not just replayed history; B9 correlated all 6 static findings as `POSSIBLE`, matching the task's own stated expectation. Both runs saved as real Past Runs (ids 14/15), visible side by side in the frontend. See MULTI_TARGET_PLAN.md's Phase 6 section for full detail.
- [x] `results/` per-target separation (done 2026-08-10, ahead of Phase 7) — the deferral noted in the Interlude and B7 auto-fill entries above, revisited once running NaViQ then Mattermost back to back (Phase 6) made it a live, visible UI bug: every block wrote a single fixed `results/{block}.json`, so the second target's run silently overwrote the first's, and `api.py`'s live-results endpoints read those same fixed paths. Fixed by generalizing B3's own `results/{target}_files_list.txt` precedent to every block via one shared helper (`result_path()` in `blocks/targets.py`), threading a `target_profile` through every block function that previously hardcoded a path, scoping B7's per-payload screenshot/video directories the same way (`results/dynamic/{target}/`, `results/videos/{target}/` — a second real collision found along the way, not just the JSON), and fixing `api.py`'s `GET /api/results` + `run_history.finish_run()` to only read/snapshot the active/owning target's own files (stripped back to canonical block names, no frontend contract change). Verified with real two-target runs, not fixtures: NaViQ (`restore`, 15 payloads executed) then Mattermost (`restore`, 10 payloads executed) back to back left both targets' full file sets intact side by side, and the live API confirmed the exact reported symptom is fixed — `GET /api/results` returned each target's own correct `total_executed` immediately after switching via `POST /api/target`. 197/197 tests green (2 new). See MULTI_TARGET_PLAN.md's "`results/` per-target separation" section.
- [x] Past Runs target display (done 2026-08-10, ahead of Phase 7) — the user's own follow-up idea right after the separation fix above. Backend already had it (`run_history.list_runs()` already selected/returned `target` per row); the gap was entirely frontend — `RunSummary` (`ui/src/lib/types.ts`) didn't declare the field at all, so it was silently dropped. Added `target: string | null` to the type and a small badge in `PastRunsView.tsx`'s `RunRow` (a local `TARGET_LABELS` map, not a fetch to `/api/target` just for a label). Live-verified with a headless-Chromium run through the real app (landing page → Past Runs tab): all 17 real historical runs show the correct badge, alternating Mattermost/NaViQ exactly matching each row's real `target` column. `tsc --noEmit` clean. See MULTI_TARGET_PLAN.md's "Past Runs target display" section.
- [x] Phase 7 — done 2026-08-10. `readme.md`: new dated paragraph in the intro changelog (2026-08-08 to 2026-08-10) plus dated updates to B1/B3/B4/B7/B13's own sections, roadmap point 1 marked superseded-in-part, test count updated 113 → 197, new closing paragraph in "Resumen ejecutivo". `fixes.txt`: new SESSION 8, the condensed technical record of every real bug found across Phases 0-6 plus today's two follow-ups (results/ separation, Past Runs target display) — full phase-by-phase detail stays in MULTI_TARGET_PLAN.md, not duplicated. See MULTI_TARGET_PLAN.md's Phase 7 section for the task-by-task breakdown.

## F. Compliance action items (from chat, not written into any doc until now)

- [ ] Ask your Profesor Coordinador: is a GitHub + Drive link acceptable instead of a physical CD/DVD for Anexo III point 3?
- [ ] Confirm with them: does the live AWS link (point 4) satisfy "software ejecutable" / "instructivo de instalación," or do they still want something separate?
- [ ] Prepare the accompanying deliverables regardless of the answer: source code link, DB/test-data export, informe files
