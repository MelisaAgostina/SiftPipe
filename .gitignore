# Docker
volumes/
backups/

# Entorno de Python
__pycache__/
*.pyc
.venv/
env/

# Configuración y seguridad
.env
.vscode/

# Logs y resultados (opcional, si los generas automáticamente)
results/

# Run history DB (blocks/run_history.py) — local run data, not project config
siftpipe_history.db

# Node
ui/node_modules/
ui/.next/
ui/dist/
ui/.tanstack/

# Playwright
playwright-report/
test-results/

# NaVi-Q source (private repo) — plain local clone, not a submodule, so
# SiftPipe's own repo never depends on NaVi-Q repo access (see
# MULTI_TARGET_PLAN.md's "Open questions" for why this isn't a submodule
# like mattermost-src/mattermost)
naviq-src/